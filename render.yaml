services:
  - type: web
    name: app-igreja-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install; npm run build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        sync: false
      - key: NODE_ENV
        value: production
